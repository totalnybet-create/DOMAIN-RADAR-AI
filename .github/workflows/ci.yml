name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      NEON_AUTH_COOKIE_SECRET: ci-only-domain-radar-auth-cookie-secret-2026
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package.json
      - name: Install
        run: npm install --no-audit --no-fund
      - name: PL Sniper tests
        run: npm run test:sniper
      - name: Typecheck
        run: npm run typecheck
      - name: Build
        run: npm run build
